import React from 'react';
import Navbar from './common/Navbar';
import Footer from './common/Footer';

const UserReviewsPage = () => {
    return (
        <div className="bg-gradient-to-b from-customBlueStart to-customBlueEnd min-h-screen"> {/* Using custom colors for the gradient */}
            <Navbar />
            
            {/* <Footer /> */}
        </div>
    );
};

export default UserReviewsPage;
